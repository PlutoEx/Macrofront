import React from 'react';
import Header from "./Header";
import BooksSearch from "./MovieSearch";

const Main = () => {
    return (
        <div>
            <Header/>
            <BooksSearch/>
        </div>
    );
};

export default Main;
